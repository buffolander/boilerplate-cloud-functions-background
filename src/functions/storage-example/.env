# DEPLOYMENT_VARIABLES # Don't remove this line
STAGE=dev
# FUNCTION_NAME The deployment script will apply the enclosing directory as function name
RUNTIME=nodejs12
MEMORY=256MB
TIMEOUT=30s
VPC_CONNECTOR=serverless-vpc-connector
TRIGGER=storage
# Your trigger bucket name
RESOURCE=serinus-reporting-v1.appspot.com
# The storage event type; accepts: finalize, delete, archive, metadataUpdate
EVENT=finalize
